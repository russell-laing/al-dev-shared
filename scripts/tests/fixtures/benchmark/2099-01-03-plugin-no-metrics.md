# Plugin Health — Fixture (no metrics block)

surface: plugin
dimensions:

- design

## Summary

New this sweep: 3 · Recurring from prior sweeps: 0 · Stale (dropped): 0 ·
Dropped (unverified): 6

This dossier predates the machine-readable metrics block, so the adapter must
surface every metric field as `not available` and must never scrape the
human-facing prose summary above as a fallback.

# Knowledge File Quality Report

Generated: 2026-06-18 04:59 UTC
Issues: HIGH (0), MEDIUM (0), LOW (4)

## HIGH Severity (Blocks Agent Guidance)

None identified.

---

## MEDIUM Severity (Incomplete Guidance)

None identified.

---

## LOW Severity (Minor/False Positives)

### handoff-chain-map.md

- **Section:** "Identified Handoff Gaps"
- **Issue:** THIN — Section has 1 introductory content line before immediately entering subsections; preamble should explain what constitutes a handoff gap and the taxonomy before presenting examples
- **Reference:** No referencing agent or skill found (documentation-only file)
- **Assessment:** False positive / orphaned file. The structural thinness is minor given the file is not actively depended upon for skill/agent execution. The section header acts as a category label, not a guidance section.
- **Fix:** Optional cleanup only. If the section is meant as an index, the single line is appropriate. Otherwise, add 1–2 sentences defining the gap taxonomy before the subsections.

---

### investigate-findings-template.md

- **Section:** "Regression Timeline"
- **Issue:** NO-CODE — Section heading contains "Timeline" which triggered the validator, but the section body contains only structured metadata fields (dates, status). Code examples for both investigation scenarios (recently-working and long-standing defect) are present in subsections "Example A" and "Example B" directly below.
- **Reference:** `profile-al-dev-shared/skills/investigate/SKILL.md` — Skill reads this template before writing findings
- **Assessment:** False positive. The code examples the validator expected at the `Regression Timeline` level exist in the child subsections. The parent-level metadata fields (dates, yes/no flags) are correctly prose-only. The validator fired on the heading keyword, not on a real gap.
- **Fix:** None required. Optional: rename heading to "Regression Timeline Metadata" to clarify the parent section is metadata-only, with examples in subsections.

---

### map-change-rubber-duck-checks.md (×2)

- **Section:** "Path variations to handle during verification" (lines 531–532)
- **Issue:** DEAD-REF — Validator flagged two references to `knowledge/file.md` as dead links
- **Reference:** No referencing agent or skill found in the shared plugin surface
- **Assessment:** False positive. Both occurrences of `knowledge/file.md` are placeholder examples in a documentation pattern block showing how relative paths vary across artifact types (e.g., `../../knowledge/file.md` vs `knowledge/file.md`). The `file.md` is a generic template variable, not a real file reference.
- **Fix:** None required. Optional: rename placeholder to `knowledge/example-file.md` or `knowledge/<name>.md` to silence the validator permanently.

---

### workflow-resilience.md

- **Section:** "Recovery After Working-Tree Loss"
- **Issue:** NO-CODE — Section heading implies procedural recovery steps but the body contains strategic guidance prose only (no fenced code blocks with git commands or recovery scripts)
- **Reference:** No referencing agent or skill found that explicitly defers to this section
- **Assessment:** Low risk. The section provides conceptual guidance ("reconstruct from source, don't search git history") with a narrative example — this is intentional. No agent defers to this file for executable recovery procedures. Adding code here could help in future but is not blocking any current workflow.
- **Fix:** Optional improvement: add a short `git` command example (e.g., `git status`, `git stash list`) to illustrate the recovery approach. Not required to unblock any agent.

---

## Fix Recommendations

### High Priority

None.

### Medium Priority

None.

### Low Priority

1. **investigate-findings-template.md** — Optional: rename the "Regression Timeline" section heading to "Regression Timeline Metadata" to make clear that the parent section is metadata-only and code examples live in subsections. Silences the validator for this file.

2. **map-change-rubber-duck-checks.md** — Optional: rename the `knowledge/file.md` placeholder at lines 531–532 to `knowledge/<name>.md` or `knowledge/example-file.md` to eliminate the DEAD-REF false positive.

3. **workflow-resilience.md** — Optional: add a short bash code block to "Recovery After Working-Tree Loss" showing a representative recovery command sequence (e.g., `git status` → `git stash list` → reconstruct pattern). Not blocking any active agent.

4. **handoff-chain-map.md** — Optional: expand the "Identified Handoff Gaps" introductory line into 2–3 sentences defining the gap taxonomy before subsections begin.

---

## High-Priority Fix Tasks

<!-- auto-generated by /audit-knowledge-quality — consumed by /fix-knowledge-quality -->

```yaml
tasks: []
```

*No HIGH-severity issues identified this run. All 4 findings are LOW-severity false positives or minor optional improvements.*
